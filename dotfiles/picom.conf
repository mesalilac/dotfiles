opacity-rule = [ 
    "75:class_g = 'kitty'",
    "95:class_g = 'discord'"
];

shadow = true;
fading = true;

shadow-exclude = [
    "class_g = 'dmenu'",
];

blur:
{
    method = "gaussian";
    size = 5;
    deviation = 5.0;
};

