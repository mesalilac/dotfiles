[module/updates]
type = custom/script
exec = checkupdates | wc -l
click-left = $TERMINAL -e sudo pacman -Syu && polybar-msg -p %pid% %exec%
format-prefix = "📦 "
interval = 3600
format-underline = ${colors.underline}